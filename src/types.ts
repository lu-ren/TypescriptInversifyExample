const TYPES = {
    Warrior : Symbol.for("Warrior"),
    Weapon : Symbol.for("Weapon"),
    ThrowableWeapon : Symbol.for("ThrowableWeapon"),
    BindedWeapon : Symbol.for("BindedWeapon")
};

export { TYPES };